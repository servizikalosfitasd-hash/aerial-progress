## Plan

1. Replace `src/assets/kalos-logo.png` with the newly uploaded image (`WhatsApp_Image_2026-04-23_at_18.40.00-Photoroom.png`).
2. Update `src/pages/Auth.tsx`: move the `<img>` logo from outside the card to inside it (as the first element, above the Google/Apple buttons), keeping it centered with appropriate spacing (e.g. `mb-6`) and a sensible max width (~220px) so it sits nicely with the other card elements.

No other files affected.